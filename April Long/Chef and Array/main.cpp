#include <iostream>
#include<stdio.h>
#include<bits/stdc++.h>

#define ll long long int
#define li long int

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    ll n,m,k;
    cin>>n>>m>>k;
    vector<ll>a(n);
    vector<ll>p(m);
    for(ll i=0;i<n;i++)
        cin>>a[i];
    for(ll i=0;i<m;i++)
        cin>>p[i];

    vector<ll>d(n);

    /*
    for(ll i=0;i<n;i++)
    {
        d[i]=rand()%k+1+a[i];
        cout<<d[i]<<" ";
    }*/
    //cout<<'\n';
\

    for(ll i=0;i<n;i++)
    {

    }



    return 0;
}
