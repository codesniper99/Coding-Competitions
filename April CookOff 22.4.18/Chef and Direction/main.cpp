#include <iostream>
#include<stdio.h>
#include<bits/stdc++.h>

#define ll long long int
#define li long int

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        vector<ll>positions(n);
        vector<ll>velocities(n);
        for(ll i=0;i<n;i++)
        {
            cin>>positions[i];
            cin>>velocities[i];
        }

        for(ll i=0;i<n-1;i++)
        {
            if(velocities[i])

        }


    }

    return 0;
}
